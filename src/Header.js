// export default function Header({ name, breed }) {
//   return (
//     <header className="App-header">
//       <p>
//         Dog {name} is {breed}
//       </p>
//     </header>
//   );
// }
// function MenuItemLink({ text }) {
//   return <div>{text}</div>;
// }

// export default function Header({ text }) {
//   return (
//     <header className="Nav">
//       {text.map((name, index) => (
//         <MenuItemLink className="right" text={name} />
//       ))}
//     </header>
//   );
// }

// ["some", "var", "exp"].map((i, index) => index);

// function Example(x = 3) {
//   x = x * 2;
//   return x;
// }

// (x) => X * 2;
// (x) => {
//   let y = 5 * x;
//   if (y === 1000) {
//     return y;

//   };
